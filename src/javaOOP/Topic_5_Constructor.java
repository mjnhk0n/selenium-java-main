package javaOOP;

public class Topic_5_Constructor {
	// A type of method, use to init the state of an object
	// No return type
	// If we dont provide any constructor in the class, java compiler provides a
	// default for that class
	// Constructor name must be the same with class name
	// Not inherited by subclasses
	// Always call it first when new an instance/ object
	// this and super keyword MUST be the first line in constructor
	// Can be overload but can't be override
	public Topic_5_Constructor() {
		// this is a constructor
	}

	public Topic_5_Constructor(String name) {
		// this is a constructor with param String ( can be int/ float / ...)
	}

}
