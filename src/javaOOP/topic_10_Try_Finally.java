package javaOOP;

public class topic_10_Try_Finally {
	// Always be executed even if the exception happens or not
	// Use to insert "clean-up" into program: close file/ close the connection (DB)
	// quit browser/ clear data test/ ...

	// With try-catch, maybe there is none or multiple Catch. But only 1 Finally
}
